{
  "schemaVersion": 2,
  "enabled": false,
  "baseUrl": "",
  "requestTimeoutMs": 5000,
  "pendingPollMs": 15000,
  "heartbeatMs": 60000,
  "note": "Standalone local-first is the default. Set mode=managed with a verified control origin only when centralized device approval is intentionally enabled.",
  "mode": "standalone"
}
