{
  "name": "price-report-control-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "wrangler deploy --dry-run --outdir dist --config wrangler.jsonc",
    "test": "node --test tests/*.test.mjs",
    "gate": "npm run typecheck && npm run build && npm test"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260515.1",
    "typescript": "5.9.3",
    "wrangler": "4.92.0"
  }
}
